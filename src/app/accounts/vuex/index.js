import * as actions from './actions';
import getters from './getters';
import mutaions from './mutations'

const state = {
  accounts: {}
};

export default {
  state,
  actions,
  mutations,
  getters
}